import {$brNotification} from '../br/br_notification';

/**
 *
 * @param tab{ browser.tabs.Tab}
 * @param info{browser.pageAction.OnClickData}
 */
export async function handlePageActionAddPlaylist(tab, info) {
  let {url: targetlink, } = tab;


}
